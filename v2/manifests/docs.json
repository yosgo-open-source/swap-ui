{
  "v": 0,
  "docs": {
    "遷移指南--docs": {
      "id": "遷移指南--docs",
      "name": "Docs",
      "path": "./src/Migration.mdx",
      "title": "遷移指南",
      "content": "import { Meta, Markdown } from \"@storybook/addon-docs/blocks\";\nimport MigrationRaw from \"../MIGRATION.md?raw\";\n\n<Meta title=\"遷移指南\" />\n\n{/* 內容單一來源:repo 根目錄 MIGRATION.md,此頁僅負責渲染 */}\n\n<Markdown>{MigrationRaw}</Markdown>\n"
    },
    "design-tokens--docs": {
      "id": "design-tokens--docs",
      "name": "Docs",
      "path": "./src/docs/DesignTokens.mdx",
      "title": "Design Tokens",
      "content": "import { Meta } from \"@storybook/addon-docs/blocks\";\nimport { ColorTokens, RadiusTokens, ShadowTokens, BreakpointTokens, FontTokens } from \"./TokenPreview\";\n\n<Meta title=\"Design Tokens\" />\n\n# Design Tokens\n\nSWAP 品牌的設計常數（色彩、圓角、陰影、斷點、字體），由 `@yosgo/swap-ui` 具名匯出。\n取代 v1 的 `Styles` 預覽小工具——v1 是 import 一個元件來「看」token，v2 直接 import「值」來用，本頁負責預覽。\n\n```tsx\nimport { swapColors, swapRadius, swapShadows, swapBreakpoints } from \"@yosgo/swap-ui\";\n\n// 搭配 sx 使用\n<Box\n  sx={{\n    color: swapColors.primary.primary700,\n    borderRadius: swapRadius.m,\n    boxShadow: swapShadows.m,\n  }}\n/>\n```\n\n同一組值也掛在 theme 上（`theme.palette.swap`），寫 styleOverrides 或 sx callback 時可直接取用：\n\n```tsx\nsx={(theme) => ({ color: theme.palette.swap.primary.primary700 })}\n```\n\n## 色彩\n\n**點擊色票可複製色碼。**\n\n<ColorTokens />\n\n## 圓角（swapRadius）\n\n<RadiusTokens />\n\n## 陰影（swapShadows）\n\n<ShadowTokens />\n\n## 斷點（swapBreakpoints）\n\n沿用 v1 的自訂斷點值（非 MUI 預設），已寫入 theme，`useBreakpoints` hook 與 `theme.breakpoints` 皆以此為準：\n\n<BreakpointTokens />\n\n## 字體（swapFontFamily）\n\n`SWAPThemeProvider` 內建自托管字體（@fontsource），不需另外引入：\n\n<FontTokens />\n",
      "summary": "# Design Tokens SWAP 品牌的設計常數（色彩、圓角、陰影、斷點、字體），由 `@yosgo/swap-ui` 具名匯出。 取代 v1 的 `Styles` 預覽小..."
    }
  }
}